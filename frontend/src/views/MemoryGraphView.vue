<!--
  记忆三元组叙事图。
  TODO: 拉取所有 NPC 的 memory_graph 三元组，
        以 subject/object 为节点，predicate 为边渲染。
-->
<template>
  <div class="placeholder-page">
    <div>
      <h2 style="color:#ce93d8;">MemoryGraphView · 记忆三元组叙事图</h2>
      <p>等待后端 <code>/api/agents/{id}/memory</code> 中的 <code>graph</code> 字段。</p>
    </div>
  </div>
</template>
